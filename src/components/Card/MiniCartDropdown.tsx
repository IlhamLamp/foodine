import { CartItems } from "@/types/cart";

const MiniCartDropdown: React.FC<{ product: CartItems }> = ({ product }) => {
    return (
        <div>
            ASD
        </div>
    )
}

export default MiniCartDropdown;